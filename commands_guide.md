# LivePad — Streamer & Viewer Guide
**Created by Cxsmo_AI**
Control Call of Duty: Black Ops Zombies or any Xbox controller PC game in real time through TikTok, YouTube, and Twitch chat.

---

## 1. Quick Start (How To Play)
**Just type what you want the character to do in chat!**
You can type one word, or string multiple words together in ONE message for combos. No passwords or direct connection needed.

### Popular Quick Combos:
- `w` — Walk forward
- `w sprint` — Sprint forward full speed!
- `ads fire` — Aim down sights and shoot weapon
- `w sprint ads fire right 40` — Run, aim, shoot, and turn right all at once!

### Say it your way
You can use plain words, keyboard shortcuts, or short chat aliases. They all do
the same thing: `walk`, `move`, `forward`, `w`, `fwd`, or `fw` move forward;
`back`, `reverse`, `retreat`, `s`, `bk`, or `bw` move backward; `strafeleft`,
`a`, or `lsleft` strafe left; and `straferight`, `d`, or `lsright` strafe right.
For combat, `fire`, `shoot`, `attack`, `atk`, `f`, or `rt` fire; `ads`, `aim`,
`zoom`, `aimdown`, or `lt` aim; and `reload`, `rel`, or `rl` reload.

---

## 2. Complete Commands Reference

| Command / Word | Action | Xbox Button | Category | What it does in-game |
| :--- | :--- | :--- | :--- | :--- |
| `w` / `forward` | Walk Forward | Left Stick Up | Move | Moves character forward |
| `s` / `back` | Walk Backward | Left Stick Down | Move | Backs away from zombies |
| `a` | Strafe Left | Left Stick Left | Move | Dodge or sidestep left |
| `d` | Strafe Right | Left Stick Right | Move | Dodge or sidestep right |
| `left` | Look Left | Right Stick Left | Aim | Turns camera left (default 40% speed) |
| `right` | Look Right | Right Stick Right | Aim | Turns camera right (default 40% speed) |
| `up` | Look Up | Right Stick Up | Aim | Aims camera up toward heads |
| `down` | Look Down | Right Stick Down | Aim | Aims camera down toward ground |
| `fire` / `shoot` | Shoot Weapon | Right Trigger (RT) | Combat | Fires equipped gun |
| `ads` / `aim` | Aim Down Sights | Left Trigger (LT) | Combat | Precision weapon zoom (600ms hold) |
| `reload` | Reload Weapon | X Button | Combat | Quick reload tap (120ms) |
| `swap` / `switch` | Swap Weapon | Y Button | Combat | Swaps to secondary or Wonder Weapon |
| `sprint` | Sprint | Left Stick Click (L3) | Move | Sprints forward (500ms lease) |
| `jump` | Jump | A Button | Action | Jumps over debris or crawlers |
| `crouch` / `b` | Crouch | B Button | Action | Ducks under hazards or drops prone |
| `slide` | Slide | Hold B (400ms) | Move | Slides forward while sprinting |
| `melee` / `knife` | Knife Attack | Right Stick Click (R3) | Combat | Knifes zombie in front of you |
| `buy` / `interact` / `use` | Buy / Open / Box | Hold X (800ms) | Action | Opens doors, buys wall guns & hits Mystery Box |
| `revive` | Revive Teammate | Hold X (1500ms) | Action | Holds interact long enough to revive |
| `nade` / `grenade` | Frag Grenade | Right Bumper (RB) | Combat | Throws lethal grenade |
| `tac` / `tactical` | Tactical Grenade | Left Bumper (LB) | Combat | Throws Monkey Bomb or Flashbang |

---

## 3. Numbers & Modifiers (Speed & Time)
No percentage (%) symbol needed! Just write the number. You can also specify seconds (`s`) or milliseconds (`ms`).

- `right 50` — Turns camera right at **50%** speed.
- `w 80` — Walks forward at **80%** stick tilt.
- `w 1.5s` — Walks forward for **1.5 seconds**.
- `w 80 1.2s` — Walks forward at **80%** strength for **1.2 seconds**!
- `fire 500ms` — Holds the shoot trigger for half a second.
- `buy 2s` — Holds interact button for 2 seconds (good for stubborn doors or Pack-a-Punch).

---

## 4. Multi-Action Combos
LivePad executes all actions in your message **simultaneously**!

- `w sprint` — Run forward full speed
- `w sprint slide` — Slide cancel / sliding sprint
- `ads fire` — Aim down sights and shoot
- `w ads fire` — Walk forward while aiming and shooting
- `w jump reload` — Jump forward and reload at the same time
- `w sprint ads fire right 40` — Sprint, aim, fire, and turn camera right

### One-word quick macros

These are shortcuts for common combinations. They create the same independent
leases as writing the words separately:

- `go` / `run` / `dash` / `rush` — `w sprint`
- `runfire` / `runshoot` / `panic` — `w sprint fire`
- `runaim` — `w sprint ads`
- `runaimfire` — `w sprint ads fire`
- `combat` / `fight` / `aimfire` / `shootaim` — `ads fire`
- `jumpfire` — `jump fire`
- `slidefire` — `w sprint slide fire`
- `reloadmove` — `w reload`
- `dodgeleft` / `dodgeright` — strafe plus sprint
- `slideleft` / `slideright` — strafe, sprint, and slide

Modifiers after a macro apply to its first action, so `run 80 1.2s` means
`w 80 1.2s sprint`. Use `+` instead of spaces if that is easier to type:
`w+sprint+ads+fire`.

Short aliases include `fwd`, `bk`, `fw`, `bw`, `sl`, `cr`, `j`, `hop`, `rl`,
`rel`, `sw`, `wpn`, `sp`, `m`, `g`, `gren`, `frag`, `t`, `i`, `pick`, `rev`,
`lt`, `rt`, `f`, `lb`, `rb`, `l3`, `r3`, `du`, `dd`, `dl`, and `dr`. A leading
`!` is optional, so `!runfire` also works.

`a` remains strafe-left. Use `jump`, `facea`, or `btn_a` for the Xbox A button.

---

## 5. Playing with a Real Controller (LivePad Extension)
Viewers who want to use a real Xbox or PlayStation controller:
1. Load the **LivePad** extension in Edge or Chrome.
2. Open the stream chat.
3. Click **ARM CONTROLLER CHAT**.
4. Moving your physical controller automatically sends chat commands!

---
*LivePad • Created by Cxsmo_AI*

